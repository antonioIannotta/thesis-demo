import pandas as pd
from proxy import Proxy
import typing


def find_proxies_by_correlation_matrix(dataset: pd.DataFrame) -> typing.Set[Proxy]:
    pass


def find_proxies_by_arm(dataset: pd.DataFrame) -> typing.Set[Proxy]:
    pass


def find_proxies(dataset: pd.DataFrame, method: str) -> typing.Set[Proxy]:
    pass
